#!/usr/bin/env bash
# bin/release <build-dir>

BUILD_DIR="$1"
STAGE_DIR=$(dirname $BUILD_DIR)
echo $STAGE_DIR
echo $HOME

cat <<EOF
---
default_process_types:
    web: PATH=$PATH:$HOME/../opt/tibco/sb-cep/7.5/bin/lv-server $1
EOF
